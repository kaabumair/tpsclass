{
  "name": "imageserver",
  "version": "1.0.0",
  "description": "an express server which as one api to return an image",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "muhammad umair",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
